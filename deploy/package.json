{
  "name": "deploy",
  "version": "1.0.0",
  "description": "Deploy DojoCRUD out of a GitHub webhook",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
